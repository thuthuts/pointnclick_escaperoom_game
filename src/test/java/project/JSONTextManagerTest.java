package project;

import org.apache.logging.log4j.LogManager;
import org.apache.logging.log4j.Logger;
import org.junit.Test;

public class JSONTextManagerTest {

    private static final Logger log = LogManager.getLogger(JSONTextManagerTest.class);

    @Test
    public void getTextTest() {

        // TODO: 30.11.20 fill in correct values when ItemText.json finalized
        /*JSONTextManager jsonManager = new JSONTextManager();
        jsonManager.parseJSON(STATICS.levelJsonTextEN[0]);

        Assert.assertEquals("Text of key item", jsonManager.getText("KEY"));
        Assert.assertEquals("Text of Card", jsonManager.getText("CARD"));
        Assert.assertNull(jsonManager.getText("CAR"));*/
    }
}
